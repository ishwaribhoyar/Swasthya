[project]
name = "cliniq"
version = "0.1.0"
description = "ClinIQ Backend"
readme = "README.md"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "app/modules"]
pythonpath = "."
